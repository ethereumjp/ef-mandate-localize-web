{
  "name": "@commentary/monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "dev:site:mock": "pnpm --filter @commentary/site run dev:mock",
    "build:site": "pnpm --filter @commentary/site build",
    "build:widget": "pnpm --filter @commentary/widget build"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "sharp"
    ]
  }
}
